# Quant Framework v3.0 — engine requirements
# Eseguire: pip install -r engine/requirements.txt

pandas>=2.0
numpy>=1.24
pyarrow>=14.0
backtrader>=1.9.78
matplotlib>=3.7      # backtrader plotting
scipy>=1.11          # statistiche walk-forward
quantstats>=0.0.62   # report performance/risk
pyyaml>=6.0
pytest>=7.4          # tests
